#Matlab style of indexing for when needed
'%m%'<-function( i,j ) {
  if ( j < i ) {
    c()
  } else {
    i:j
  }
}

# #This adjusts the width of the R output to match the used screen in Linux
# .adjustWidth <- function(...){
#        options(width=Sys.getenv("COLUMNS"))
#        TRUE
# }
#  
# .adjustWidthCallBack <- addTaskCallback(.adjustWidth)