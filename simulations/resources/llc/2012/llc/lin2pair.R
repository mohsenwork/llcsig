lin2pair <- function(P,ind) {
  #Indexing function for switching btw. B and b.
  #INPUT:
  # P - indexing structure (often eqs$P)
  # ind - index of the vector b
  #OUTPUT:
  # c(i,j) index of B

	ij <- P[,ind]
	ij
}